export default function DashboardDefault() {
  // This file exists only so the (dashboard) route group
  // has a default render target when no parallel route is selected.
  // It should render nothing.
  return null;
}